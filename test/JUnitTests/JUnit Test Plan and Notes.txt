Tests

DatabaseUpdater

	void updateRelevantNodes(String article, Map<String, Integer> relatedArticles)
		- null article
		- null map
		- article with non-alphanumeric characters
		- alphanumeric article
		- Map with all the same objects (nonunique) objects in it
		

	void updatePreviewText(String article, String summary)
		- null article
		- null summary
		- strings with nonalphanumeric chars

	void updateImageURL(String article, String articleURL)
		- null article
		- null articleURL

	void RemoveArticle(String article)
		- null article
	
DumpUpdater
	Notes: this class has many public methods that should be private
	
	boolean download(String downloadFileURL, String outputFileName)
		- null inputs
		- invalid inputs
		- valid inputs
	
	boolean compareTimestamp(String previousTS,String latestTS)
		- null inputs
		- invalid (non-numeric) inputs
		- valid inputs
	
	int convertToIntMonth(String month)
		- null input
		- invalid input
		- valid input
	
	boolean writeTimestamp(String timestamp, String  timestampLog)
		- null inputs (returns false)
		- invalid (non-numeric) inputs (returns false)
		- valid inputs (returns true)
	
	String getPreviousTimestamp(String timestampLog)
	
	String getLatestTimestamp()
	
	BufferedReader read(String url)
		- invalid URL
		- valid URL
		
	
WikiParser
	ArticleVector calculateRelationships(String name, String text)
	
	void calculateRelevancy()
	
	String getPreviewText(String text)
	
	String getImageUrl(String text)

ArticleVector
	Notes: this class should have getters and setters
	
	String articleName
	
	list<String> links
	
RelationshipBuilder
	void build(Map<String, ArticleVector> parsedArticles) 
		
	
	
	
	
	
	